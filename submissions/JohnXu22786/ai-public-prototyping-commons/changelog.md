# 方案迭代记录

# Changelog — ai-public-prototyping-commons

## v0.1 - 2026-08-29

## 2026-08-29 — Codex completion round

- Consolidated the taskbook delivery into one independent concept: three scopes, three positionings, five functions, internal three areas/two wings, agent.1–6 evidence, six bounded global mechanism cases, eight-element AI governance, ten scenario cards, three industry protocols, and six personas including older, access-needs, caregiver and child/student groups.
- Added the traceability index in `report/narrative.md`, with explicit boundaries for all 13 review dimensions, evidence status and the `unknown/to_verify` register.
- Added bilingual offline visual indexes with local figure references, exact machine metric attributes and rounded provisional human displays; added figure alt text and rights-ledger provenance.
- Kept official geometry, cooperation, approval, operational baseline and performance claims provisional or `unknown/to_verify`; no external partnership or statutory planning claim is made.

## 2026-08-29 — CocoSgt 5057228517 remediation round

- Replaced the BLOCK71 404 with the Arduino Education official page and reduced the formal global mechanism count to five verified primary pages; 2050 remains an explicitly excluded research lead pending title verification.
- Recorded the actual repository-local Noto Sans SC render input, its version metadata and SHA-256, archived the complete SIL OFL-1.1 text and documented the exact upstream archive-match boundary; the TTF is not placed under package assets because strict validation disallows that extension.
- Changed manifest data confidence from high to mixed and scoped it to structural/package validation; spatial and operational claims remain provisional, low, medium or unknown/to_verify.
- Regenerated bilingual figures and A0/A3 drawings after the English layout correction; HTML font embedding and manifest hashes are refreshed after rendering.

## 2026-08-29 — CocoSgt 5057513241 remediation round

- Unified the structured primary brand to `PAR·JZ`: corrected `A-RIGHTS-001`, the agent.1/agent.5 logo traceability paths, and the rights ledger so every package reference resolves to the manifest-registered `assets/figures/logo-parjz.png`.
- Closed the source traceability loop by removing the unsupported `SOURCE-REGISTRY` and `PROCESSED-FACT-PACK` labels from all source-id arrays in `compliance_matrix.json`, `standard_matrix.json`, and `design_depth_matrix.json`; the remaining source IDs resolve to canonical records in `sources.json`, with `unresolved_source_ids` empty.
- Refreshed manifest hashes and persisted the passing four-gate self-check; no drawings or rendered bilingual surfaces required regeneration because the repair changed only structured traceability and rights metadata.

This package is intended for review and community discussion only. It is not a statutory plan, approval, procurement, investment or construction document.
