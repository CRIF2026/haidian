# N1 direct continuation audit

This package continues the earlier CRAFT-JZ proposal into the review-ready submission directory. The core narrative is bilingual and contains thirteen fixed sections, three positions, five functions, three zones, two wings, three concept nodes, twelve scenario cards, four test protocols, six personas, a spatial-AI-operations loop, implementation phasing, metrics, risk, sources, and rights evidence.

The three formal visual indicators are finite values recomputed from the participant-submitted geometry and repeated in `visual/index.html` data attributes. All three are labelled low-confidence provisional participant model values. FAR, height, ownership, funding, cooperation, approvals, capacity, demand, and operating outcomes remain unknown or to_verify.

The intended review state is `ready_for_review` only after the four local gates, strict validation, schema checks, font coverage, visual checks, PDF checks, and manifest hashes are all green. This status is a local package claim and does not pre-judge organizer review or any external reviewer verdict.
